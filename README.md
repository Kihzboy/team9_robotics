# Obstacle detecting rover

The robot has 2 core scenarios:
1. Maze-solving robot
2. Obstacle avoidance robot

Both files should be uploaded to root directory of the jetson.
Then the needed one file can be executed via ssh using "python3 maze.py" or "python3 avoider.py" command.
